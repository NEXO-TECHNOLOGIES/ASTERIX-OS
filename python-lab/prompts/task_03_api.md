# Task 03: API Client Logic

You need to fetch JSON from a public endpoint and print one field from the response.

Requirements:
- perform a real HTTP request
- decode JSON safely
- set a reasonable User-Agent header
- handle timeouts gracefully

Focus on:
- HTTP semantics
- JSON parsing
- resilient client behavior
- request hygiene
