# Task 02: Shell Automation

Write a Python script that runs a system command and returns the output cleanly.

Requirements:
- use a real system command, not a fake stub
- capture stdout properly
- avoid unnecessary shell escaping
- return or print the result in a clean format

Focus on:
- subprocess use
- shell safety
- command output handling
- OS-level reasoning
