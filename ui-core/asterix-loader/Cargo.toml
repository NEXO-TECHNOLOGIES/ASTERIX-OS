[package]
name = "asterix-loader"
version = "1.0.0"
edition = "2021"
authors = ["ASTERIX OS Project"]
description = "High-performance cybernetic boot visualizer and control center for ASTERIX OS"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
# Zero external runtime dependencies for maximum portability across Linux, Termux, and Bare-Metal!
